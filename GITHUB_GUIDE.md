# 📖 GitHub Guide: Etymology AI

คู่มือสำหรับการจัดการโค้ดและทำงานร่วมกับ GitHub ในโปรเจ็กต์ **Etymology AI** สำหรับผู้เริ่มต้นและระดับกลาง

---

## 🛠 1. การตั้งค่าเริ่มต้น (Initial Setup)

ก่อนเริ่มใช้งาน ต้องตรวจสอบว่า Git ตั้งค่าชื่อและอีเมลของคุณถูกต้อง

```bash
# ตรวจสอบการตั้งค่า
git config --list

# ตั้งค่าชื่อและอีเมล (ทำครั้งเดียว)
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

### การเชื่อมต่อกับ Repository
โปรเจ็กต์นี้เชื่อมต่อกับ: `https://github.com/Eakkachad/etymology_ai.git`

```bash
# ตรวจสอบว่าเชื่อมต่อกับ Remote ไหนอยู่
git remote -v
```

---

## 🔄 2. วงจรการทำงานปกติ (Daily Workflow)

ขั้นตอนมาตรฐานเมื่อคุณแก้ไขโค้ด:

### ขั้นตอนที่ 1: ดึงโค้ดล่าสุด (Pull)
ทำทุกครั้งก้อนเริ่มงานเพื่อป้องกันโค้ดตีกัน (Conflict)
```bash
git pull origin main
```

### ขั้นตอนที่ 2: ตรวจสอบการเปลี่ยนแปลง (Status)
```bash
git status
```

### ขั้นตอนที่ 3: เลือกไฟล์ที่จะบันทึก (Add)
```bash
git add .                           # เลือกทั้งหมด
git add path/to/file.py             # เลือกเฉพาะไฟล์
```

### ขั้นตอนที่ 4: บันทึกการเปลี่ยนแปลง (Commit)
เขียนข้อความอธิบายสั้นๆ ว่าแก้สู่อะไร
```bash
git commit -m "feat: เพิ่มโมเดล Siamese Network"
```

### ขั้นตอนที่ 5: ส่งขึ้น GitHub (Push)
```bash
git push origin main
```

---

## 🌿 3. การใช้งาน Branch (แนะนำสำหรับการแก้ฟีเจอร์ใหญ่)

การใช้ Branch ช่วยให้คุณลองผิดลองถูกได้โดยไม่กระทบโค้ดหลักที่รันบน DGX

```bash
# สร้าง Branch ใหม่
git checkout -b feature/new-model

# สลับกลับไป main
git checkout main

# รวมโค้ดจาก branch เข้า main
git merge feature/new-model
```

---

## ⚠️ 4. ข้อควรระวังสำหรับโปรเจ็กต์นี้ (Project Specifics)

### สิ่งที่ **ห้าม** Push ขึ้น GitHub
1. **Data ขนาดใหญ่**: ไฟล์ใน `data/raw/*.json` หรือ `data/processed/*.pt`
2. **Checkpoints**: ไฟล์ใน `outputs/checkpoints/*.ckpt`
3. **Environment**: โฟลเดอร์ `venv/` หรือ `.conda/`

> [!IMPORTANT]
> ไฟล์เหล่านี้ถูกตั้งค่าไว้ใน `.gitignore` แล้ว ห้ามไปลบไฟล์ `.gitignore` ออกเด็ดขาด

### การจัดการ SLURM Scripts
หากคุณแก้ไขไฟล์ `.slurm` ใน `scripts/slurm/` อย่าลืม commit และ push ทุกครั้ง เพราะ DGX จะดึงโค้ดจากที่นี่ไปรัน

---

## 🆘 5. วิธีแก้ปัญหาเมื่อเกิด Conflict

ถ้าคุณ `git pull` แล้วเจอข้อความ `CONFLICT (content): Merge conflict in...`

1. เปิดไฟล์ที่มีปัญหา
2. มองหาเครื่องหมาย `<<<<<<< HEAD` และ `>>>>>>> ...`
3. เลือกว่าจะเอาโค้ดส่วนไหนไว้ (หรือรวมกัน)
4. ลบเครื่องหมาย `<<<<`, `====`, `>>>>` ออกให้หมด
5. บันทึกไฟล์แล้วรัน:
   ```bash
   git add <ชื่อไฟล์>
   git commit -m "fix: resolve merge conflict"
   git push origin main
   ```

---

## 💡 Tips สำหรับ Eakkachad

- **Commit บ่อยๆ**: ช่วยให้ย้อนกลับไปเวอร์ชันเก่าได้ง่ายขึ้น
- **ใช้ VS Code Git UI**: หากไม่ถนัด Command Line, VS Code มีปุ่ม Source Control (ไอคอนสามเหลี่ยมเชื่อมกัน) ที่ใช้งานง่ายมาก
- **Token**: หาก GitHub ถามรหัสผ่าน ให้ใช้ **Personal Access Token (PAT)** แทนรหัสผ่านปกติ

---
*คู่มือนี้สร้างขึ้นเพื่อช่วยให้คุณจัดการโปรเจ็กต์ Etymology AI ได้อย่างยั่งยืน*
