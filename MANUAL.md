# คู่มือการใช้งานโปรเจกต์ Etymology AI อย่างละเอียด

**Neural Phonetic Mapping & Etymological Discovery**
ระบบวิเคราะห์และทำนายความสัมพันธ์ทางรากศัพท์ระหว่างภาษาไทย (คำยืมบาลี-สันสกฤต) กับตระกูลภาษาอินโด-ยูโรเปียน

---

## 📚 สารบัญ

- [คู่มือการใช้งานโปรเจกต์ Etymology AI อย่างละเอียด](#คู่มือการใช้งานโปรเจกต์-etymology-ai-อย่างละเอียด)
  - [📚 สารบัญ](#-สารบัญ)
  - [1. ภาพรวมของโปรเจกต์ (Project Overview)](#1-ภาพรวมของโปรเจกต์-project-overview)
  - [2. สถานะปัจจุบัน (Current Status)](#2-สถานะปัจจุบัน-current-status)
  - [3. โครงสร้างไฟล์และโฟลเดอร์ (Project Structure)](#3-โครงสร้างไฟล์และโฟลเดอร์-project-structure)
  - [4. การติดตั้งและเตรียมความพร้อม (Installation)](#4-การติดตั้งและเตรียมความพร้อม-installation)
    - [4.1 Prerequisites](#41-prerequisites)
    - [4.2 ขั้นตอนการติดตั้ง](#42-ขั้นตอนการติดตั้ง)
  - [5. คู่มือการใช้งาน (User Guide)](#5-คู่มือการใช้งาน-user-guide)
    - [5.1 การสร้างข้อมูลตัวอย่าง (Generating Sample Data)](#51-การสร้างข้อมูลตัวอย่าง-generating-sample-data)
    - [5.2 การทดสอบแปลงสัทอักษร (Phonetic Conversion Demo)](#52-การทดสอบแปลงสัทอักษร-phonetic-conversion-demo)
    - [5.3 การดาวน์โหลดข้อมูล (Downloading Data)](#53-การดาวน์โหลดข้อมูล-downloading-data)
    - [5.4 การใช้งานใน Python Code](#54-การใช้งานใน-python-code)
  - [6. การตั้งค่าระบบ (Configuration)](#6-การตั้งค่าระบบ-configuration)
    - [6.1 `configs/data_sources.yaml`](#61-configsdata_sourcesyaml)
    - [6.2 `configs/model_config.yaml`](#62-configsmodel_configyaml)
  - [7. แผนงานในอนาคต (Roadmap)](#7-แผนงานในอนาคต-roadmap)

---

## 1. ภาพรวมของโปรเจกต์ (Project Overview)

โปรเจกต์นี้มีเป้าหมายเพื่อสร้างเครื่องมือ Computational Tool ที่สามารถ "ทำนาย" และ "สืบค้น" ความสัมพันธ์ทางรากศัพท์ (Etymology) ได้ โดยเน้นไปที่:
*   **คำไทย** ที่ยืมมาจาก **บาลี-สันสกฤต**
*   เชื่อมโยงกลับไปยัง **Proto-Indo-European (PIE)**
*   และเปรียบเทียบกับคำในตระกูลยุโรปอื่นๆ (เช่น อังกฤษ, ละติน, กรีก)

ระบบใช้เทคนิค Deep Learning (Phonetic Embeddings, Siamese Networks, GNN) เพื่อวิเคราะห์ความคล้ายคลึงของเสียงและสร้างกราฟความสัมพันธ์

## 2. สถานะปัจจุบัน (Current Status)

ขณะนี้โปรเจกต์อยู่ใน **Phase 1: Data Collection & Phonetic Processing**

*   ✅ **เสร็จสมบูรณ์**:
    *   ระบบแปลงคำเป็นสัทอักษร (IPA Conversion) สำหรับภาษาไทย, สันสกฤต, และภาษาตะวันตก
    *   ระบบดาวน์โหลดข้อมูลจากแหล่งต่างๆ (Kaikki, WOLD, etc.)
    *   โครงสร้าง Config และการออกแบบ Model Architecture
*   🚧 **กำลังดำเนินการ/ยังไม่เริ่ม**:
    *   การเทรนโมเดล (Training)
    *   โมเดล AI (Neural Networks ใน `src/models`)
    *   การแสดงผลกราฟ (Visualization)

---

## 3. โครงสร้างไฟล์และโฟลเดอร์ (Project Structure)

```text
etymology_ai/
├── configs/                  # ไฟล์ตั้งค่าระบบ
│   ├── data_sources.yaml     # ตั้งค่าแหล่งข้อมูล (URL, API)
│   └── model_config.yaml     # ตั้งค่า Model Hyperparameters (ยังไม่ได้ใช้จริงในโค้ด)
├── data/                     # โฟลเดอร์เก็บข้อมูล
│   └── raw/                  # ข้อมูลดิบที่ดาวน์โหลดมา หรือ sample data
├── notebooks/                # Jupyter Notebooks สำหรับการทดลอง
├── scripts/                  # สคริปต์ Python สำหรับรันคำสั่งต่างๆ
├── src/                      # Source Code หลัก
│   ├── data/                 # โค้ดจัดการข้อมูล
│   │   ├── downloaders.py    # ระบบดาวน์โหลดข้อมูล
│   │   ├── phonetic_converter.py # หัวใจหลัก: ระบบแปลง IPA และ Extract Features
│   │   └── sample_dataset.py # สคริปต์สร้างข้อมูลจำลอง
│   ├── models/               # (ยังว่าง) ที่เก็บ Model Class
│   ├── training/             # (ยังว่าง) Script สำหรับเทรน
│   └── visualization/        # (ยังว่าง) โค้ดแสดงผลกราฟ
└── ...
```

---

## 4. การติดตั้งและเตรียมความพร้อม (Installation)

### 4.1 Prerequisites
*   OS: Linux (แนะนำ) หรือ macOS/Windows
*   Python 3.8+
*   Anaconda (แนะนำ)

### 4.2 ขั้นตอนการติดตั้ง

1.  **Clone Project** (ถ้ายังไม่ได้ทำ)
    ```bash
    cd /home/67070309/eak_project/etymology_ai
    ```

2.  **สร้าง Environment และติดตั้ง Dependencies**
    วิธีที่ 1: ใช้ Conda (แนะนำ)
    ```bash
    conda env create -f environment.yml
    conda activate etymology
    ```

    วิธีที่ 2: ใช้ Pip
    ```bash
    pip install -r requirements.txt
    ```

    *หมายเหตุ: หากไม่มีไฟล์ `environment.yml` ให้ใช้ pip install ตามปกติ*

---

## 5. คู่มือการใช้งาน (User Guide)

### 5.1 การสร้างข้อมูลตัวอย่าง (Generating Sample Data)
เนื่องจากยังไม่มี Database จริง เราสามารถสร้างไฟล์ JSON ตัวอย่างเพื่อทดสอบระบบได้

**คำสั่ง:**
```bash
python src/data/sample_dataset.py
```
**ผลลัพธ์:**
*   สร้างไฟล์ `data/raw/sample_etymology_data.json`
*   ประกอบด้วยคำศัพท์ตัวอย่าง เช่น "มารดา" (ไทย) -> "mātṛ" (สันสกฤต) -> "*méh₂tēr" (PIE) -> "mother" (อังกฤษ)

### 5.2 การทดสอบแปลงสัทอักษร (Phonetic Conversion Demo)
สคริปต์นี้จะโชว์ความสามารถหลักของระบบในตอนนี้ คือการแปลงคำศัพท์ภาษาต่างๆ ให้เป็น IPA (International Phonetic Alphabet) และคำนวณระยะห่าง (Distance)

**คำสั่ง:**
```bash
python scripts/demo_phonetic_conversion.py
```
**สิ่งที่แสดง:**
*   โหลดข้อมูลจาก `sample_etymology_data.json`
*   แปลงคำไทยเป็น IPA (เช่น มารดา -> /mɑː.dɑː/)
*   แปลงคำสันสกฤตเป็น IPA (Custom Logic)
*   แปลงคำอังกฤษ/ละตินโดยใช้ library `epitran` (ถ้ามี)
*   คำนวณ Phonetic Distance เพื่อดูความใกล้เคียง

### 5.3 การดาวน์โหลดข้อมูล (Downloading Data)
สคริปต์ `download_sample_data.py` ใช้สำหรับดึงข้อมูลจากแหล่งภายนอก (ตามที่ config ไว้)

**คำสั่งตัวอย่าง:**
```bash
# ดาวน์โหลดข้อมูลภาษาไทยจาก Kaikki
python scripts/download_sample_data.py --source kaikki --language thai --limit 1000

# ดาวน์โหลดข้อมูลภาษาไทยจาก WOLD (World Loanword Database)
python scripts/download_sample_data.py --source wold --language thai
```
*(หมายเหตุ: ฟังก์ชันบางส่วนใน downloaders.py อาจยังเป็น Placeholder ต้องมีการเขียน scraping logic เพิ่มเติมสำหรับบางแหล่ง)*

### 5.4 การใช้งานใน Python Code
คุณสามารถเรียกใช้ `PhoneticConverter` ในโปรแกรมของคุณได้ดังนี้:

```python
from src.data.phonetic_converter import PhoneticConverter

# 1. เริ่มต้น Converter
converter = PhoneticConverter()

# 2. แปลงคำไทย
thai_ipa = converter.to_ipa("อัคคี", "tha")
print(f"อัคคี -> {thai_ipa}")
# ผลลัพธ์: อัคคี -> ak.kʰiː (โดยประมาณ)

# 3. แปลงคำสันสกฤต
san_ipa = converter.to_ipa("agni", "san")
print(f"agni -> {san_ipa}")
# ผลลัพธ์: agni -> agni

# 4. หาความเหมือน (Distance ยิ่งน้อยยิ่งเหมือน)
dist = converter.phonetic_distance(thai_ipa, san_ipa)
print(f"Distance: {dist}")
```

---

## 6. การตั้งค่าระบบ (Configuration)

### 6.1 `configs/data_sources.yaml`
ไฟล์นี้คือกุญแจสำคัญในการจัดการแหล่งข้อมูล
*   **kaikki**: ตั้งค่า URL ของไฟล์ JSONL ที่ดึงมาจาก Wiktionary
*   **wold**: ตั้งค่า URL และ Language ID สำหรับ World Loanword Database
*   **starling**: ตั้งค่าแหล่งข้อมูล PIE Roots
*   **panlex**: ตั้งค่า API Key หรือ Rate Limit

### 6.2 `configs/model_config.yaml`
ใช้ออกแบบโครงสร้าง Model ในอนาคต (Phase 3)
*   **phonetic_embedding**: กำหนดขนาด Vector, Layer ของ Transformer
*   **siamese_network**: กำหนดโครงสร้าง Network สำหรับเปรียบเทียบคำ 2 คำ
*   **etymology_gnn**: กำหนดค่า Model Graph Neural Network

---

## 7. แผนงานในอนาคต (Roadmap)

สิ่งที่ต้องทำต่อจากนี้เพื่อความสมบูรณ์ของโปรเจกต์:

1.  **Implement Models**: เขียนโค้ดใน `src/models` ให้ตรงกับ Config
2.  **Training Pipeline**: เขียนสคริปต์เทรน Model ใน `src/training` โดยใช้ข้อมูลที่เตรียมไว้
3.  **Graph Construction**: สร้างกราฟความสัมพันธ์ของคำจริงใน `src/data` หรือ `src/inference`
4.  **Web Interface**: สร้างหน้าเว็บสำหรับค้นหาและแสดงผล (Phase 5)

---
ผู้จัดทำ: *Eakkachad & Antigravity Assistant*
ฉบับล่าสุด: 2026-02-07
