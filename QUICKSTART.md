# วิธีใช้งาน Etymology AI - Quick Start Guide

## ✅ สิ่งที่ทำให้เรียบร้อยแล้ว

1. ✅ ติดตั้ง dependencies แล้ว
2. ✅ สร้างข้อมูลตัวอย่าง (sample data) แล้ว
3. ✅ ทดสอบระบบแปลงสัทอักษร (IPA Conversion) แล้ว

## 🚀 คำสั่งสำหรับใช้งาน

### 1. สร้างข้อมูลตัวอย่าง
```bash
cd /home/67070309/eak_project/etymology_ai
python3 src/data/sample_dataset.py
```
ผลลัพธ์: สร้างไฟล์ `data/raw/sample_etymology_data.json` ที่มีคำศัพท์ตัวอย่าง 8 คำ

### 2. รันโปรแกรมแปลงสัทอักษร (Demo)
```bash
python3 scripts/demo_phonetic_conversion.py
```
ระบบจะแสดง:
- การแปลงคำภาษาต่างๆ เป็น IPA
- การคำนวณระยะห่าง (Phonetic Distance) ระหว่างคำ
- ตัวอย่างความสัมพันธ์ระหว่างภาษา

### 3. ใช้งานผ่าน Python Code
```python
# เปิด Python REPL หรือสร้างไฟล์ .py ใหม่
import sys
sys.path.insert(0, '/home/67070309/eak_project/etymology_ai/src')

from data.phonetic_converter import PhoneticConverter

# สร้าง Converter
converter = PhoneticConverter()

# ทดสอบแปลงคำสันสกฤต
sanskrit_word = "mātṛ"  # แม่
ipa = converter.to_ipa(sanskrit_word, 'san')
print(f"{sanskrit_word} → IPA: {ipa}")

# ทดสอบแปลงคำอังกฤษ (ถ้ามี epitran)
# english_word = "mother"
# ipa_eng = converter.to_ipa(english_word, 'eng')

# คำนวณระยะห่าง
dist = converter.phonetic_distance("maːtr̩", "maːter")
print(f"Distance: {dist}")
```

## 📝 หมายเหตุสำคัญ

### ปัญหาที่พบและวิธีแก้

1. **Thai IPA Conversion ยังไม่สมบูรณ์**
   - pythainlp 4.0.2 (Python 3.8 compatible) ไม่สามารถใช้งานได้เต็มที่
   - ทางเลือก: อัพเกรดเป็น Python 3.9+ แล้วติดตั้ง pythainlp 5.x

2. **epitran และ panphon ยังไม่ได้ติดตั้ง**
   - ถ้าต้องการแปลงภาษาอื่น (อังกฤษ, ละติน) ให้รัน:
   ```bash
   pip install epitran panphon
   ```

## 🎯 สิ่งที่ใช้งานได้แล้ว
- ✅ การแปลง Sanskrit → IPA (ใช้งานได้เต็มที่)
- ✅ การคำนวณ Phonetic Distance (Levenshtein)
- ✅ Sample data generation
- ⚠️ Thai → IPA (ต้องอัพเกรด Python หรือใช้ romanization แบบอื่น)

## 🔜 ขั้นตอนต่อไป (ถ้าต้องการพัฒนาต่อ)
1. Download ข้อมูลจริงจาก Kaikki
2. สร้าง Model สำหรับทำนาย cognates
3. สร้างกราฟความสัมพันธ์ระหว่างภาษา

---
**สร้างเมื่อ**: 2026-02-07
**Python Version**: 3.8
**Dependencies**: pythainlp 4.0.2, pandas, numpy, pyyaml, requests
